import WasteManagementComponent from './waste-management';

const WasteManagement = () => {
  return (
    <div className="flex max-h-[200vh] w-full flex-col items-center justify-center gap-8 bg-[#EFEDDE] text-center">
      <WasteManagementComponent />
    </div>
  );
};

export default WasteManagement;
